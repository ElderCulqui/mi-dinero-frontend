services:
  mi_dinero_app:
    image: node:22-alpine
    container_name: mi_dinero_app
    working_dir: /app
    volumes:
      - .:/app:rw
      - root_mi_dinero_app_data:/app/data
    env_file: .env
    expose:
      - "${PORT}"
    ports:
      - "${PORT}:5173"
    command: sh -c "apk add --no-cache git && npm install && npm run dev"
    networks:
      - local_network
    restart: always

networks:
  local_network:
    external: true
    name: local_network

volumes:
  root_mi_dinero_app_data: